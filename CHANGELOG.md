# Changelog

## v0.2.0 (unreleased)
- Webhook 配置 CRUD（控制面订阅配置闭环）：补齐 create/update/delete 端点与前端管理页——上一任务 10 处事件已接线但配置唯一生产写入点是 V0.4.4 seed 的 3 行 INSERT，/webhooks 页只读自述"创建走 API/seed"（其实连 API 都没有），用户无法从控制面订阅任何事件。端点挂 WebhookController（`POST/PUT/DELETE /api/webhooks/game/{gameId}/configs[/{configId}]`，201/200/{deleted:true}，不存在/归属不符 404，校验失败 IAE→400），鉴权走 AccessGuard 行内 `webhook:manage`（现有 5 个 GET/test 端点与 @PreAuthorize 注解维持现状不动）；Service 三方法照 MetricAlertService 样板：id `wc_`+UUID(24)、校验（name 必填≤100/url 必填 http(s)≤500/httpMethod 白名单 POST|PUT|PATCH/authType 白名单 none|basic|bearer|api_key——hmac/oauth2 发送侧无实现拒绝配置防"配置成功但鉴权头静默不发"/authType≠none 时 authConfig 必须非空 JSON object/eventTypes/riskLevels 逗号分隔规范化/timeout 1-300/maxRetries 0-10/backoff ≥100）+ 全量审计（CREATE/UPDATE/DELETE 三态前后快照）+ 软删（webhook_logs 对配置是无级联裸 FK 只能软删）**且软删同时置 INACTIVE**（重试队列 processPendingRetries 只查 isActive 不查 deletedAt，不置 INACTIVE 在途日志会继续轰炸已删 URL）；update 为部分更新语义，authConfig null/blank → 保留原值（secret 不回显前端，留空即不改；authType/authConfig 按"最终生效组合"校验——patch 改为需鉴权类型但留空时沿用 existing 旧值判定）；实体 authConfig 加 @JsonIgnore（GET 列表/详情不再回显明文 secret，发送侧读 DB 实体不走 JSON 零影响，objectMapper 审计序列化同源排除——secret 不落审计日志）；权限种子 V0.9.6（`webhook:read`/`webhook:manage`，display_order 136/137 续 V0.9.3 的 134/135；operator/game_admin 双权限 + analyst/finance/viewer/marketing/qa/developer 只读，显式 INSERT..SELECT 绑定——V0.2.3 的批量绑定不覆盖新权限）；前端 /webhooks 页改造：页头"新建配置"+ 行内编辑/删除（confirm）/启用停用（PUT {...config, status}）+ 创建/编辑共用弹层（name/webhookUrl 必填、eventTypes 常用事件提示、authType 下拉 + 非 none 时 authConfig JSON textarea（编辑时 placeholder"已设置，留空保持不变"）、timeout/maxRetries/backoff 数值输入带范围提示、风险级别过滤），400 展示 error.message + 顶部成功条，副标题/空态删"创建走 API / seed"措辞。测试：WebhookServiceTest 新增 8 用例（创建默认值与 CSV 规范化/显式 null 字段穿透实体初始化器时服务侧兜底/校验矩阵含 name>100 与 url>500/更新 secret 留空保留与新值覆盖/重名拒绝/软删+INACTIVE+审计/审计序列化失败吞异常不阻断/api_key 配置非字符串 key 发送侧 CCE 告警不阻断），WebhookService 328 行、WebhookController 23 行覆盖均 100%。**边界声明：authConfig 明文存储无加密（DB TEXT 列现状，本次不引入加密）；GET 不再回显 secret（编辑留空=保留）；hmac/oauth2 authType 校验拒绝（发送侧无实现）；requestHeaders/requestTemplate/retryConfig 高级字段不走表单（API 直建仍可携带）；sendCustomWebhook 按 eventTypes 逗号分隔匹配为既有语义；webhook_logs 随 30 天自动清理保留**
- FeatureFlag 灰度/条件语义补全（此前 STAGED_ROLLOUT/CONDITIONAL 两态在 `isAvailableForUser` 直接回落 defaultValue——设 50% 灰度后所有用户拿 false，灰度永远放不出去，check 接口静默失效不报错；advanceRollout 只递增 currentStep 不回写百分比）：STAGED_ROLLOUT 态改 FNV-1a 确定性分桶 `toUnsignedLong(hash32(flagKey + ":" + userId)) % 100 < percentageValue`（复用 ExperimentSplitter.hash32 并改 public——与四端 SDK 同源算法，分桶可独立复算审计；percentageValue null 按 0、userId 空/blank 回落 defaultValue），曝光率精确等于配置百分比且同用户结果稳定；advanceRollout 按 rolloutSteps（JSON 整数数组）步进回写百分比——复用 setPercentage 钳制与状态联动（末步 100 自动转 ENABLED），步骤列表非法 JSON/含越界值/为空/已超步数时仅递增不回写（与既有行为一致）；CONDITIONAL 态补最小条件求值：conditions 为 JSON 数组 AND 语义 `[{"attribute":"user_id"|"game_id","op":"eq"|"ne"|"in"|"not_in"(大小写不敏感),"value":标量或数组}]`（value 的 Number/Boolean 转字符串比对），conditions 空/null/非法 JSON/空数组/对象 → 回落 defaultValue（保守），未知 attribute/未知 op/value 为空/入参 null → 该条件按 false（明确不放行不回落）。**行为变更：白名单全状态生效——灰度/条件态白名单用户此前拿 defaultValue（TODO 副产品），现恒放行（命中优先于分桶/条件判定）**。前端新增功能开关管理页（web/src/views/FeatureFlagsView.vue，路由 /feature-flags——统计四卡/开关表格（状态徽章/灰度进度条+当前步/操作列），启用/禁用就地更新 + 灰度设置弹层（百分比输入，0/100 自动转 DISABLED/ENABLED 提示，400 错误展示），调既有 enable/disable/percentage 端点）。测试：EntitiesDeepTest 追加灰度分桶锚定向量/边界/白名单先行/回写与容错用例 + 新增条件求值全分支用例，OpsServicesDeepTest 新增 check 端到端分桶用例，FeatureFlagEntity 行覆盖 100%。**边界声明：flag_type 仅展示维度，行为唯一由 flag_status 决定；STAGED_ROLLOUT 态不叠加 conditions 求值（曝光率= bucket% 可审计）；check 端点 `GET /api/system/features/{flagKey}/check` 维持无鉴权现状（零消费方）；黑白名单 String.contains 子串匹配为既有缺陷（"u1" 误伤 "u10"）不在本次范围；功能开关无 create/update 全字段端点（创建走 API/seed），前端页仅启用/禁用/灰度三操作**

- Webhook 完善（test 真发送 + 通知派发接线）：修复 `POST /api/webhooks/test/{configId}` 假成功（此前 TODO 直接返回 test_initiated，不发 HTTP 不记日志）——新增 `WebhookService.sendTestWebhook` 同步直发、绕过事件/风险级过滤（测试即验证 URL 可达与鉴权头）、webhook_logs 照常落库但不进重试队列、不写配置成功/失败统计（连续失败自动停用的逻辑不可由测试触发），service 层校验 config.gameId 归属（IAE→400），返回恒 200 + body 携带投递结果；接线 10 处业务事件派发（此前全部只有 TODO 注释）：玩家/设备/IP 封禁 block（V0.4.4 种子声明此类型却从未发过）、审核 SLA 逾期 review_sla_breach 与超 24h 自动升级 review_escalation、系统告警升级 alert_escalation、配额警戒/耗尽 quota_warning/quota_alert、维护窗口即将开始 maintenance_upcoming 与应结束未结束 maintenance_ending、导出完成 export_complete、模型漂移 model_drift、SDK 版本 30 天内退役 sdk_version_retiring（平台级走 DEFAULT 配置）。接线前置修复两处洪泛缺陷：MaintenanceService 两定时任务此前无状态变迁（每分钟重复触发），改用 notification_sent（既有列此前零业务读写，存量 NULL 行首轮各补发一次）+ 新增 end_notification_sent（V0.9.5）做一次性守卫，状态机零改动；ReviewQueueService.checkEscalations 此前从不写 escalated 标志（每小时重复轰炸），现置位 escalated/escalatedTo/escalatedAt/escalationReason（不动 reviewStatus，不把处理中项挪出审核工作流）。RestClientConfig 的 RestTemplate 补全局超时（connect 3s / read 5s，oddsmaker.webhook.*-timeout-ms 可配）——派发同步内联在调用方线程（含 @Scheduled 单线程池），无超时一个挂起端点会拖死全部定时任务。前端新增 Webhook 通知页（web/src/views/WebhooksView.vue，路由 /webhooks——统计卡/配置列表/测试按钮/发送日志弹层），WebhookLogEntity.webhookConfig 补 @JsonIgnore（LAZY 反向字段在 open-in-view: false 下 GET logs 序列化必 500）。**边界声明：sendCustomWebhook 不按环境过滤（environmentId 恒 null，与既有 metric_alert 一致），绑定环境的配置会收到所有环境事件；维护窗口/系统告警 gameId 为空时只守卫不派发（不伪造路由），仅 SDK 版本退役走 DEFAULT 平台级配置；model_drift 漂移持续期每 6h 重发、sdk_version_retiring 30 天窗口内每日重发（无持久化去重标记）；quota 告警在 HTTP 请求线程派发，首次跨阈值请求最多增加约 8s 延迟；test 端点允许测停用配置；webhook_configs.timeout_seconds 列仍不消费（全局默认代替）；配置 CRUD 端点仍不存在（创建走 API/seed），前端页只读+测试+日志**
- 可配置漏斗端到端断链修复：打通"控制面配置 → Flink 执行 → 结果查询展示"全链——此前控制面漏斗 CRUD 写 funnel_configs（V0.8.8 按实体建），而 Flink funnels-job 读 funnel_analyses（V0.3.3 建的唯一写入者是 e2e seed），用户在控制面建漏斗后 job 启动即 "No funnel configurations found. Exiting." 退出；同一张 funnel_steps 表两套列语义并存（控制面写 funnel_id/time_window_sec/optional，job 查 funnel_analysis_id/time_from_previous/is_optional）控制面写的步骤 job 永远查不到；job 结果写 CH funnels_configurable 但 control-service 零查询、前端无页面（/api/funnels 零调用）。修复：job 改读 funnel_configs/funnel_steps(funnel_id)（enabled/deleted_at 表达启用态，type 枚举直接匹配无序判定，time_window_sec 缺省/非法回退 24h，optional 可空回退 false，user_key 透传）；V0.9.4 迁移放宽 funnel_steps.funnel_analysis_id 为可空（此前控制面建带步骤的漏斗在 PG 上必然 NOT NULL violation——测试全绿因 test profile flyway disabled + H2 create-drop；FK 保留，PG 外键不约束 NULL）；FunnelStepEntity.funnel 补 @JsonIgnore（open-in-view: false 下 GET /api/funnels 序列化 LAZY 反向字段必抛异常且存在递归）；新增结果端点 `GET /api/funnels/{funnelId}/results?gameId=&environment=&days=`（FunnelResultsService 聚合 funnels_configurable 按步 sum(users) 并按步重算转化率，funnel.gameId 归属校验防跨游戏探测，AccessGuard game:read 行内鉴权，CH 未配置 available=false 降级）；前端新增漏斗分析页（web/src/views/FunnelsView.vue，路由 /analytics/funnels——列表/创建/编辑（步骤动态行）/启停/删除 + 结果条形图与明细表）。e2e-verify.sh 第 15 节 seed 同步改写 funnel_configs/funnel_steps(funnel_id)（DELETE 双条件兼容清理老环境 funnel_analyses 残留）。**边界声明：funnel_analyses 降级为死表留档（与 retention_analyses 同类，不 DROP，既有环境无感）；job 配置为启动时一次性快照——建/改/停/删漏斗均需重启 configurable-funnels-job 才生效；SummingMergeTree 口径 users = 按日去重用户数的跨日累加（同一用户跨天窗口内重复计入），行级 conversion_rate 恒 100 不可聚合、真实转化率由结果端点按步重算；控制面 userKey 被 job 读取但未参与分组（分组键固定 user_id→device_id 回退）；optional 步骤仅对 SEQUENTIAL/TIME_WINDOW 生效；@JsonIgnore 后 /api/funnels 响应中 step 不再内嵌 funnel 对象**
- 数据保留策略自动化（CH TTL 读 dataRetentionDays 配置）：打通控制面配置到 ClickHouse 表级 TTL 的消费链路——game/environment 的 dataRetentionDays 此前零消费方（环境"继承自游戏配置"注释无实现），CH 侧 TTL 硬编码且混乱（events/game_events 365 天、exp_daily_* 400 天，sessions/identities/risk_* 全家/retention/funnels 等 ~17 张完全无 TTL 无限保留）；有效保留天数 = env.dataRetentionDays ?? game.dataRetentionDays ?? 全局默认(90)（实现既有继承/覆盖约定），表级期望值 = 全部游戏与环境有效保留天数的最小值（CH 表全库共享不分 game，TTL 是表级物理属性，最严格承诺生效——数据最小化合规安全方向），clamp 到 [7, 3650]（min-days/max-days 可配，防误配 0 立即清光数据；GameService 保存游戏/环境时对非 null 值同范围校验 400）；RetentionEnforcementService 定时对账（默认每小时 40 分错峰）——读 system.tables 的 engine_full 正则解析当前 TTL（容忍反引号列名与 DELETE 后缀），漂移则逐表 ALTER ... MODIFY TTL 修正（metadata 操作不产生 mutation，materialize_ttl_after_modify 默认开启由 CH 后台重写既有 parts）；仅管理 21 张明细/事实表白名单（events/game_events/sessions/identities/risk_events/risk_scores/risk_actions/retention 三表/funnels 三表/exp_* 八表 + exp_first_level_complete），排除 15 张 inline MV 聚合草图内表（uniqState 语义特殊不可逆重建）、predictions（业务自管 expires_at）、item_dim/level_dim（配置维度）；逐表 try-catch 隔离，有 TTL 但解析失败置 UNKNOWN 拒绝盲改（防每轮反复 ALTER 死循环），表缺席 UNKNOWN 提示；状态一行一表落 retention_enforcements（actual/desired/status/修正次数/最近检查，V0.9.3 迁移含 retention:read/manage 权限种子显式绑定 operator/game_admin 全量 + 六角色只读）；API `/api/retention/enforcements`（状态查看）与 `/api/retention/enforcements/run`（手动对账，不受调度门限制），AccessGuard 全局权限鉴权 + ALTER 全量审计（有变更才记一条汇总）；前端数据保留页（概览三卡 + 21 表状态表格 + 手动对账，web/src/views/DataRetentionView.vue，路由 /data-retention）。**边界声明：定时对账 enabled 默认 false（ODDSMAKER_RETENTION_ENABLED=true 开启）——首次开启会为 ~17 张无 TTL 表下发 MODIFY TTL，超过保留期（默认 90 天）的历史数据将被 CH 后台清理且不可逆，请确认配置后再启用；多游戏部署取最严格 min，新增游戏默认 90 天会拉低全局 TTL（请显式配置）；MODIFY TTL 触发的后台 parts 重写在大表上有 IO 负载，逐表串行且默认错峰执行**
- 全仓覆盖率巡检收口（全部模块 LINE 口径 ≥98%：control 99.6%、gateway 99.1%、flink 七 job 全部 ≥98.5%）：上一轮死代码定性清单复核后新补四组——①control Application main 入口以 mockStatic(SpringApplication) 完成（上轮误定性为死代码，直线代码正常完成即记覆盖，与 flink/gateway main 同法）；②FeatureFlagEntity flagStatus=null 时四状态谓词全 false 安全回落 defaultValue 兜底 + onCreate 补 DISABLED、SDKVersionEntity onCreate 为 null 字段补 DRAFT/PATCH（字段初始化器使 new 后非 null，需显式置 null 才可达守卫）；③PermissionService 三域循环体落空回边（现有"无效角色"用例走 continue 的 goto 不经过循环闭括号，需 valid+enabled 但无目标权限的角色走完循环体）；④gateway BatchController.sampledIn 的 SHA-256 不可用降级（mockStatic(MessageDigest) 线程局部——SpringBootTest 真实 HTTP 跑在 Netty 线程不生效，须测试线程内反射直调私有方法，与端到端用例分离）。剩余 <98% 文件全部为严格论证的物理不可达（定性更新：BatchController 10 行 → 8 行——parseEvents 三路径对 null 元素全有守卫、normalizeCompatFields 先行推断 eventType 致主循环推断分支恒假、parseEpochMillis 三调用点 hasNonNull 守卫、toJsonSilently 反序列化 POJO 不抛、parseMode/parseIpMode 调用点非 null 守卫、getPolicy 因 HmacFilter 已验 key 恒非 null；PermissionService 4 行 isActive/isLocked 互斥短路；WebhookConfigEntity 2 行 String.split 对非 null 输入不抛的防御 catch；ExperimentSplitter 取模兜底——h % totalWeight < totalWeight 数学恒真；ReplayGuard synchronized 编译器异常出口）
- 玩家数据删除请求（GDPR erasure）：输入任一标识（player_id/user_id/device_id）经身份图谱展开全部关联标识（identities + identity_links 不动点，seen 集合防环，AnyStatus 查询收入非活跃/已删行，直匹配兼容无 link 历史行；超 `oddsmaker.erasure.identity-expansion-limit`(500) 置 truncated 仍执行已解析子集），展开结果创建时固化存 resolved_identities；异步清洗由 sweep 每 30 秒驱动——PG 段短事务硬删明细（identities 物理删而非软删、identity_links、player_login_logs、player_payments、redeem_records、op_mail_claims、player_export_jobs 含磁盘导出文件先行删除），op_mails 个人邮件仅摘除收件人 token（正文是运营资产且列表可能含其他玩家，摘空保留空串行），风控处置记录（risk_cases/review_queues/block_lists，block_lists 仅玩家标识三类）匿名化占位 `erased:<reqId>` 保留防欺诈审计线索；ClickHouse 段逐表提交 ALTER ... DELETE mutation（events/sessions/identities/risk_events/risk_scores/risk_actions/exp_exposure_users/exp_first_level_complete/predictions 共 9 表，谓词尾 `/* per:<reqId> */` 标记，字面量单引号转义防注入，提交前 count() 预估），system.mutations 轮询采用基线法确认（提交前记录各表未完成数基线，回落后即确认——不依赖 command 文本，注释被剥离也能判定），超时（默认 300s）不判失败收口 COMPLETED + confirmed=false（mutation 已持久化必然最终执行）；CH 未配置 → SKIPPED_UNCONFIGURED 整体完成，CH 提交异常 → PARTIAL 自动重试（默认上限 8 次，重试只走 CH 段——pgDone 中间落库防 PG 段重复；PG 段异常 → FAILED，因图谱已物理删除无法重新展开）；刻意不加类级事务：PG 删除尽早持久化，CH 失败不能回滚 PG；僵尸 PROCESSING 由 started_at 超时（默认 60 分钟）重置回 PENDING（全流程幂等）；宽限期预留 scheduled_for 列（null=立即，可指定未来时间错峰）；API `/api/privacy/erasure-requests`（创建/列表/详情/取消，AccessGuard 鉴权 `privacy:read/privacy:manage`，权限种子经 V0.9.2 迁移显式绑定 operator/game_admin 全量 + 六角色只读），取消仅限 PENDING；完成/失败经 WebhookService 派发 `player_data_erasure` 事件并全量审计；前端隐私合规页（请求列表状态徽章 + 新建表单 + resolved_identities 折叠面板 + execution_summary 详情弹层，web/src/views/PrivacyView.vue）。边界声明：Kafka 残留（7 天 retention）与 Flink checkpoint（90 天 TTL）自然过期，gateway 不落盘，CH 聚合草图表（uniqState 草图无成员语义不可逆删个体），audit_logs/users 内部表正当保留，备份不可逐条追溯
- 业务指标告警（新增功能）：按游戏配置 DAU/收入/崩溃率的告警规则（条件二选一——绝对阈值（gt/lt）或较昨日同时段的同比偏差（±N%，方向可仅下跌/仅上涨/双向），评估窗口 TODAY/HOUR_1），定时评估（默认每 10 分钟，`oddsmaker.metric-alert.enabled/cron` 可配）后站内告警 + Webhook 通知（复用 WebhookService，event_types 含 `metric_alert` 的 active 配置，notificationSent 语义为"已派发"，投递结果看 webhook_logs）；沿触发抑制——同一规则存在活跃告警（OPEN/ACKNOWLEDGED/INVESTIGATING/SNOOZED）时仅累加 occurrenceCount 不重发，恢复只回写规则 last_state=OK 不自动 resolve（人工处理闭环）；规则 CRUD/手动试算（不改状态）/告警历史/确认/解决 API（`/api/games/{gameId}/alert-rules|alerts`，AccessGuard 行内鉴权 `alert:read/alert:manage`，权限种子经 V0.9.1 迁移——id 用冒号运行时可匹配、code 用点号显示惯例，显式绑定 operator/game_admin 全量 + analyst/finance/viewer/marketing/qa/developer 只读）；Flyway V0.9.1 metric_alert_rules 表（id 冒号坑规避：eval_window 列名——window 是 PG 保留字）+ system_alerts 补 game_id/rule_id 列；前端业务告警页（规则表格/新建表单/行内启停/试算/删除 + 告警历史确认解决弹层，web/src/views/AlertsView.vue）
- 修复 @EnableScheduling 全仓缺失（plan 外发现）：control Application 此前只有 @SpringBootApplication+@EnableKafka，18 个 Service 的 30 个 @Scheduled 方法从未被调度执行（e2e 全绿因关键路径由 API 直接触发）；补 @EnableScheduling 并配 `spring.task.scheduling.pool.size: 4`（防慢任务饿死单线程调度器）；两处高风险模拟调度加默认关闭的配置门——HealthMonitorService.collectSystemMetrics（Math.random 假指标每分钟落库且约每 10 分钟制造假 CRITICAL 告警）与 performScheduledHealthChecks（随机把 health_checks 翻成 UNHEALTHY）分别由 `oddsmaker.health.simulated-metrics-enabled/simulated-checks-enabled`（默认 false）控制；其余休眠调度器（公告上下线/封禁到期解封/Webhook 重试/导出处理/实验指标聚合等）评估为低风险保留
- 全仓文件级行覆盖率 98% 收尾攻坚（<98% 文件全部补测至达标或定性死代码收口）：①control 22 个文件补测——MLModelService 七处序列化失败兜底（createTrainingJob/updateTrainingProgress/completeTraining/cancelTraining/deployModel/configureAbTest/recordPrediction/completePrediction）、ReportService 报表参数序列化失败与超时/清理任务异常吞掉、FlinkJobService 配置序列化抛出与部署时规则 ID 解析容错、HealthMonitor DISK 指标触盘告警与四定时任务异常吞掉、PipelineService 质量规则 stopOnFailure 执行路径、SecurityService 会话清理任务异常、PermissionService 三域非法角色遍历、PlayerDataQueryService ingest 校验与 limit 兜底、RiskDashboard 默认参数、PlayerExportService 下载读文件失败、SymbolicationService 堆栈概要空行/省略行跳过、RetentionMetrics/Finance/PaymentFunnel 空输入归一、GameEntity.markNotNew 与 RateLimitPolicy 用量为 0 兜底、AccessGuard 普通 viewer 迭代；②gateway CrashFingerprinter 两处——"N more frames" 省略行分支（JaCoCo 将 continue 的 goto 归属该行，"...N more frames" 被 startsWith 短路致分支永缺）与 SHA-256 算法不可用降级（mockStatic MessageDigest 触发 NoSuchAlgorithmException catch，真实可发生的 FIPS/Provider 故障路径）；③flink 五个 job 的 main 入口以 mockStatic(StreamExecutionEnvironment)+RETURNS_DEEP_STUBS 替身执行环境完成（main 为直线代码，行覆盖探针在方法 return 前，必须正常完成才记覆盖——替身化使 execute 不触达真实集群）；ConfigurableFunnelsJob 另以 mockStatic(DriverManager)+JDBC 三层替身走通控制库漏斗配置装载（ResultSet 先 true 后两次 false 驱动主/步骤双查询）；RetentionJob main 同法；④RawEvent 数值字段为不可解析字符串归 null（lng/dbl 的 parse catch）、SessionsJob.deterministicId(null) 异常兜底、Enrichers 坏 mmdb 文件 IOException 回退与 mmdb 路径存在时 DatabaseReader.Builder mockConstruction（显式 stub 链——RETURNS_DEEP_STUBS 在 final 类链（CityResponse.getCountry）上断链返回 null 不可用）；flink 五模块新增 mockito-core 5.15.2 测试依赖。遗留物理不可达死代码定性保留：BatchController 10 行（parseEvents 双路径过滤 null、normalizeCompatFields 已推断 eventType、SHA-256 catch、调用点 hasNonNull/WebFlux remoteAddress/HmacFilter 401 契约保证的防御守卫、toJsonSilently 反序列化 POJO 不抛、parseMode/parseIpMode 调用点 null 守卫）、ReplayGuard synchronized 块闭括号（编译器异常出口 monitorexit 行）、control Application main 入口、PermissionService isLocked 与 isActive 互斥短路、ExperimentSplitter 62、AuditLogEntity/WebhookConfigEntity/FeatureFlagEntity/SDKVersionEntity 穷尽 default 与字段初始化使守卫不可达
- 覆盖率最低文件定向攻坚（control 98.10% → 98.46%）：SecurityConfig 49.28% → 100%（mock HttpSecurity 的 Customizer lambda 体从不执行——真实 HttpSecurity 构建需共享 ApplicationContext 并注册 jwtDecoder 单例；反射取 ExceptionTranslationFilter 处理器实测 401/403 JSON；jwtDecoder 四分支直测，issuer-uri 分支 Nimbus withIssuerLocation().build() 会立即拉取 OIDC 发现文档并预取 jwks_uri，需本地 HttpServer stub + 程序化 RSA-2048 JWKS）；AnnouncementController/PlayerExportController/FunnelStepEntity → 100%（404 分支矩阵、PrePersist 主键生成直测）；AuditLogEntity → 97.50%（唯一剩余 miss 为 switch 穷尽全部枚举后不可达的 default 死代码，保留作防御兜底）
- 四端 SDK 与后端契约一致性攻坚（静态审计坐实 5 项漂移 + 1 项功能差距，web 为参照系全端对齐）：①币种大小写分裂——android/iOS/unity 的 revenue/currency 币种未统一大写，财务报表按币种分组会分裂，三端全部 uppercase 对齐 web；②player_id 顶层字段缺失——除 web 外三端只在 props 携带，Online/Finance 指标主体口径（player>user>device）被降级为 device 口径，三端 Event 新增 14 个顶层契约字段（player_id/level_id/game_mode/order_id/product_id/virtual_currency/virtual_amount/item_id/resource_id/resource_amount/flow_type/ad_network/ad_placement/ad_format），track/revenue 重构统一 queueEvent 入口；③iOS URL(relativeTo:) 丢 endpoint 子路径——反代部署下 flush 请求路径错，改为与 web/unity 一致的字符串拼接；④unity FetchExperimentsCached 返回 JsonUtility 无法序列化的 Dictionary 包装 "{}"（删除错契约死类型 ExperimentConfig/Variant/Targeting，缓存改为 timestamp+data 原文存取，命中回调 data 原样内容）；⑤四端对 HTTP 200 响应体中的逐事件拒绝全部静默丢弃——统一新增 handleBatchResponse/ParseKafkaErrorIds/flushHandleBatchResponse：仅 kafka_error（临时故障）回队队首发重试并持久化，invalid_schema/blocked 等永久拒绝丢弃+告警，响应体不可解析按全成功（幂等既有行为）；⑥typed helpers（levelStart/Fail/Complete、currencySource/Sink、itemGrant/Consume、iapOrder/webshopOrder、adImpression）从 web 下放到三端，props 中 game_mode/network/placement_id/ad_format 自动提升顶层。测试：android 45/45（行覆盖 98.85%）、iOS 53/53（llvm-cov 95.51%，未命中行均为 #if 条件编译与属性声明，新增逻辑行全覆盖）、unity 46/46、web 44/44（98%+）；iOS 测试基建修复——127.0.0.1:1 端口模拟连接失败在受限网络下 SYN 被丢、completion 分钟级才回调（既有用例时序侥幸），http_stubs.py 新增 status 0（写非法状态行后断开，确定性传输层失败）
- gateway config/kafka 组件测试补齐：PII 清洗全分支（邮箱/手机/IP 三模式与 Overrides、嵌套深度裁剪、denyKeys）、JsonSchemaValidator 校验矩阵（必填/长度/枚举/oneOf 类型）、PropsPolicy 白名单与大小限制、AuthService 本地回退/缓存/上下文判定、AvroPublisher 全字段映射与主题路由、DlqPublisher 载荷三分支与转义（producer 注入 mock）；config 包指令覆盖率 89.4%、kafka 包 76.3%（剩余为需真实 broker 的 producer 初始化分支）
- 测试覆盖率 98% 攻坚完成（control 模块 84.1% → 98.0%，测试总数 783 → 1040 全绿）：九轮定向补齐——①service 尾部深分支（MLModel/Experiment/TrackingPlan/Permission/SecurityService/GameService/HealthMonitor/Pipeline/RiskDashboard/DeveloperPortal/UserService/IdentityService/MailService/AuditLogService 等的异常 catch/状态机/回退分支）；②jpa 实体业务方法二轮深测（状态判定/标记/生命周期回调/148 个嵌套枚举）；③DTO 转换全字段映射（fromEntity/toEntity/updateEntity/builder）；④security 组件（AdminTokenFilter 全分支/Keycloak JWT 转换/PermissionAspect 切面/AccessGuard 三级 scope/AuditLogInterceptor IP 回退链）；⑤config（SecurityConfig 过滤链构建/CORS/JWT 解码器/OpenAPI/公司上下文）；⑥GlobalExceptionHandler 全 20 个 handler；⑦ApiController 全端点委托 + ExperimentResultsController.ingestMetrics 幂等回填；⑧风控事件分发/封禁身份扩散/Webhook 重试失败路径；⑨游戏状态机全迁移对/导出完成通知/Cohort 计算失败回滚/CH 客户端构造分支。gateway 43 测试不变，全仓库 1040 测试通过
- 测试覆盖率攻坚（control 模块 29.8% → 60.3%）：实体/DTO 反射冒烟覆盖（jpa 70.5%）；遗留 Controller 补齐 27 个（按域分 6 个测试类：运营/分析/风控/平台/管线集成 + ControlService，api 包 84.7%）；GlobalExceptionHandler 全异常映射 + AuditLogInterceptor 注解审计；看板/运营/基础设施三批 Service 主路径测试（Analytics/RiskDashboard/Webhook/ReviewQueue/BlockList/Cohort/Maintenance/Report/Export/FlinkJob/Integration/HealthMonitor/RateLimit/Pipeline/Security/AuditLog/DeveloperPortal）；control 测试总数 253 → 411，全量通过
- 实验、Crash 和智能化（P6）：①实验平台补齐 SRM 样本比例偏差检验——卡方拟合（实际分桶 vs 配置权重），p 值经正则化不完全 gamma（级数/连分式 + Lanczos logGamma）精确计算并 clamp 下溢，`/api/experiments/{id}/results` 输出总体与分指标 SRM（检测阈值 0.001，检出时应先排查分流/作弊再解读指标）；②Crash/Error 全链路——Gateway 新增 CrashFingerprinter：error 类型事件在 PII 清洗后注入 crash_hash（堆栈前 8 帧规范化：剥离 at 前缀/十六进制地址/行号数字/文件路径 basename，SHA-256 取 16 hex，SDK 已提供则保留）与 crash_message，供 ClickHouse v_crash_top_groups 聚合；符号化引擎 SymbolicationService（V0.8.5 为 symbol_mappings 增加 mapping_rules 正则规则列，按 (game,platform,version) 匹配 ACTIVE 映射依序替换，坏规则容错跳过）；Crash 聚合 API `/api/crash-metrics/{gameId}/top-groups|trend|rate-by-version|symbolicate` + 前端 Crash 监控页（Top 分组表/趋势折线/版本崩溃率/在线符号化工具）；③pLTV 预测——D7→D30 乘数法（成熟 cohort 的累计 D30/D7 收入比率均值作乘数，外推未成熟 cohort），`/api/ltv-metrics/{gameId}/pltv`；④流失预测——v_user_features_30d 特征 → ChurnScorer 启发式打分（不活跃天数主因子 0.7 + 30 天会话衰减 0.3 + 付费用户 -0.1 缓冲，输出可解释 reasons 与 high/medium/low 分级）→ 归档 ClickHouse predictions（type=churn，TTL 30 天，写失败不阻断），`/api/prediction-metrics/{gameId}/churn[/refresh]`；⑤风险评分模型——risk_events 30 天严重度加权（CRITICAL×4/HIGH×3/MEDIUM×2/LOW×1，40 分制归一）聚合主体模型分，归档 predictions（type=risk_model）与规则分互补；⑥Remote Config/LiveOps——V0.8.6 remote_configs 表（(game,env,key) 唯一、version 自增、软删），运营 CRUD + SDK/游戏服拉取 `/api/remote-config/{gameId}`（环境特定覆盖全环境、聚合版本 ifNoneMatch 304 增量）；⑦测试覆盖率——根构建引入 JaCoCo（test 后自动生成 XML/HTML/CSV 报告），P5/P6 新代码指令覆盖率 control 98.3% / gateway 96.7%，control 全量测试 342 个通过
- 报表与数据看板增强（P5）：四个 ClickHouse 数据源报表 API + 前端看板页——①留存趋势（`/api/retention-metrics/{gameId}/trend`，retention_daily 按天/周/月 cohort 对齐输出新增与 D1/D7/D30 留存率趋势，SummingMergeTree 行合并、D30 汇总仅计成熟 cohort 防低估）；②付费漏斗（`/api/payment-metrics/{gameId}/funnel`，注册（首见）→首充→二充（付费事件数 ≥1/≥2）→月留存（注册后 1-30 天活跃）总体漏斗 + cohort 明细，月留存以成熟 cohort 为分母）；③实时在线监控（`/api/online-metrics/{gameId}`，近 N 分钟有事件上报的独立主体数（player>user>device 兜底），按平台/版本/渠道（attribution.channel 回退 platform）分组 + 分钟趋势，维度 SQL 表达式白名单防注入）；④财务报表与 CSV 导出（`/api/finance-metrics/{gameId}/report|export`，按日/月输出新增/DAU/收入/付费/订单 + ARPU/ARPPU/付费率，CSV RFC4180 转义 + UTF-8 BOM，导出全量审计）。行映射抽 4 个纯函数 Assembler（17 用例）；CH 未配置统一降级 available=false。前端新增留存趋势/付费漏斗/在线监控/财务报表四个看板页（零依赖 SVG TrendChart 折线 + 漏斗横条 + 分组条形，30s 自动刷新，游戏选择器 localStorage 持久化），路由与侧边栏导航同步注册
- 玩家数据导出工具（P4）：按 (gameId, playerId) 打包导出玩家全量数据——四分区（profile 档案含充值/登录汇总、payments 充值流水、login-logs 登录日志、redeem-records 兑换记录）可全选或子集；json 导出单文件、csv 按分区打包 zip（RFC 转义）；任务化异步执行（sweep 每 30 秒处理 PENDING，失败标记 FAILED 保留错误信息）；文件落盘目录与保留时长可配（`oddsmaker.player-export.storage-dir/retention-hours`，默认 72h），cleanup 每日到期清理（标记 EXPIRED + 删文件），下载时二次校验状态与过期兜底；API `/api/player-exports`（创建/列表/详情/下载，game:read 鉴权 + EXPORT 审计）；Flyway V0.8.4 player_export_jobs 表
- 风控大屏接口（P2）：新增 `/api/risk-metrics/{gameId}/trend|rule-hits|severity|actions`，数据源 ClickHouse risk_events + risk_actions（AccessGuard 按 risk_rule:read 鉴权）——风险趋势（时间桶×严重等级透视，≤48h 按小时/更长按天自动分桶，缺失等级补零）、规则命中（rule_id 聚合命中数/影响主体数/平均分/最近命中）、严重等级分布（severity + risk_type 双维）、处置状态（action/state 聚合 + 最近处置明细）；行映射抽纯函数 RiskMetricsAssembler 可脱离 CH 单测；CH 未配置时统一降级返回 available=false 空数据
- ClickHouse risk_actions 处置归档（P2）：新增 risk_actions 表（MergeTree，(game_id, environment) 分区，与 risk_events 经 risk_event_id 关联，含 risk_case_id/action/state/source/detail）；Control Service 处置链路（RiskEventConsumer 的 BLOCK/REVIEW/MARK/THROTTLE/ALERT/WEBHOOK 分支）处置后经 RiskActionRecorder 写入 risk_actions 并联动更新 risk_scores 主体最新风险分（ReplacingMergeTree 保留最新，reasons 数组经 createArrayOf 写入）；归档为旁路能力，CH 不可用或写失败仅记日志不影响处置主链路；Control 新增 ClickHouseClient（消费 CLICKHOUSE_URL/USER/PASSWORD 部署变量，留空自动降级）+ clickhouse-jdbc 0.6.5 依赖；新增 RiskMetricsAssemblerTest/RiskActionRecorderTest 共 14 用例、RiskEventConsumerTest 增补归档断言
- 事件契约 v1 收口（P0）：修复 Gateway→Kafka topic 契约断裂——`oddsmaker.kafka.topic` 配置键与 `AvroPublisher` 读取的 `oddsmaker.kafka.topic.events` 不匹配导致配置失效，且默认 topic（`oddsmaker-events`/`oddsmaker-risk-events`）与 Flink/Control 消费端（`oddsmaker.events_raw`/`oddsmaker.risk_events`）不一致，统一为点号命名并修正配置键；Gateway 兼容层显式剔除废弃路由字段 `tenant_id`/`org_id`（不参与映射，仅携带废弃字段的事件按 invalid_schema 拒绝，防 Event 模型未来字段回退）；Gateway 内嵌 Avro schema 与主 schema（schema/avro/oddsmaker-event.avsc）完全同步，契约单一来源；新增 EventContractV1Test 覆盖废弃字段忽略与拒绝路径
- 玩家数据查询（P4）：按 playerId 跨游戏档案聚合（identity 基本数据 + 充值/登录汇总，行级按 game:read 权限过滤）；充值记录（player_payments，游戏服上报 (game_id, order_id) 唯一幂等 + 并发唯一约束兜底，仅 COMPLETED 计入累计）；登录日志（player_login_logs 追加上报）；查询/上报 API `/api/player-data/*`；Flyway V0.8.3
- 兑换码系统（P4）：UNIQUE 一次性码批量生成（Crockford Base32 去混淆字符集）/ SHARED 通用码（指定或生成，总量限次）；防刷三层（每玩家限领计数、批次总量、有效期与状态）+ UNIQUE 条件更新原子核销 + (batch, player, seq) 唯一约束幂等兜底；兑换返回奖励快照凭据；游戏服 redeem/history API（跨游戏码统一按 invalid_code 处理防探测）；运营端批次 CRUD/停用/码导出 + Flyway V0.8.2 三表
- 运营邮件系统（P4）：全服（惰性展开）与个人（精确收件人匹配）邮件；附件 JSON 校验（type/id 必填）；领取凭据固化附件快照且幂等（唯一约束 + 并发兜底）；sweep 每 5 分钟将到期已发送邮件标记 EXPIRED；游戏服经 inbox/claim API 拉取与代领
- 公告系统（P4）：全生命周期管理——创建（草稿/定时）、立即发布、改期、手动下线、软删除；sweep 每分钟扫描驱动 SCHEDULED→PUBLISHED 与 autoOfflineAt 到点下线；游戏服经 `GET /api/announcements/active` 拉取展示窗口内公告（环境匹配或全环境）；状态机约束（OFFLINE 不可复活、PUBLISHED 不可直接删）；增删改与自动迁移全量审计
- 留存分析补齐 Rolling 口径：RetentionPolicy 抽取纯逻辑（可配 N-Day `retention.ndays`、Rolling `retention.rolling.ndays`）；Rolling 语义为第 N 天及以后任意一天活跃，最后活跃日跨越阈值时补记；新增 `retention_rolling` CH 表
- 漏斗补齐无序口径：FunnelType 新增 UNORDERED；STANDARD/UNORDERED 走任意顺序完成判定（UnorderedFunnelLogic：全步骤完成 + 时间跨度约束，超窗重置，每用户一次转化）；SEQUENTIAL/TIME_WINDOW 保持原顺序推进逻辑
- 事件类型推断补全九类：inferEventType 新增 user/resource/design 映射（session/user/business/resource/progression/design/error/ad/risk + experiment 附加）
- Identity Merge、商业化（IAP/广告/LTV 视图）、玩法分析（关卡进度/经济流转视图）经核对已具备，勾选完成
- 实验平台（A/B 测试）闭环：ExperimentSplitter 确定性分流（SHA-256(salt+subjectId) 分桶 + 权重分配，SDK/服务端算法一致）；服务端分流 API `GET /api/experiments/{id}/assign`（非 running 实验兜底 control）；指标快照接收 API（聚合管道按窗口幂等回填每变体 count/sum/sumSquares/successes）；结果 API 输出比例 z-test / Welch t-test 检验（lift、95% CI、p 值、显著性、小样本 low_power 提示），多变体以 control_variant 为基线两两对比
- Gateway 风控前置：新增 ReplayGuard（签名重放 401 replay_detected、event_id 幂等吸收计入 duplicates），事件时间戳信差 ±24h 检查（invalid_timestamp）；黑名单/签名时间窗/非法环境/body size 此前已具备
- Flink risk job 新增两类检测：DUPLICATE_RECEIPT（同 subject 同 receipt_hash/order_id 窗口内 ≥2 次，CRITICAL/REVIEW）、AD_REWARD（激励广告 reward 窗口超频，HIGH/ALERT）；RuleConfig 默认兜底同步扩展
- 风控 Webhook 闭环：BLOCK/REVIEW/MARK/THROTTLE 处置后统一通过 `risk_action` webhook 输出到游戏服；REVIEW 升级为创建 RiskCase 进入审核队列（CRITICAL 优先级 1）；新增 MARK 动作分发
- 公司内 RBAC 落地：六角色权限矩阵（owner/operator/analyst/developer/risk_admin/viewer），支持 global/game/environment 三级 scope 分配与精确回收；新增 `/api/users/{userId}/role-assignments` API，GRANT_ROLE/REVOKE_ROLE 全量审计
- 修复失效鉴权：RiskRuleController/ReportController 的 `@PreAuthorize(hasAuthority(...))` 无 authority 供给（实际永远拒绝），替换为 AccessGuard 显式 scope 检查；SecurityException 统一映射 403
- 审计日志补齐密钥与环境资源：API Key 创建/策略变更/删除、环境创建/更新/删除全量记录（含变更前后值）
- Tracking Plan 字段字典规格化：属性定义新增 `cardinalityLimit` 上限（防高基数字段打爆存储）；ENUM 类型强制要求非空、无重复的 allowedValues JSON 数组且 cardinalityLimit ≥ 候选数；ARRAY 强制声明 arrayElementType
- 属性定义补齐 update/delete 接口（draft 计划内可编辑，软删除），增删改全量审计
- 新增 TrackingPlanServiceTest 10 个用例覆盖字段字典校验矩阵
- 环境策略绑定下发执行：Control 内部接口透出 `envStatus/envEnableSampling/envSampleRate`；Gateway 对非 active 环境返回 503 `environment_unavailable`，环境级确定性采样按 device_id SHA-256 分桶（同设备事件同进同出，保漏斗/留存口径），响应新增 `sampled_out` 计数
- 采样分桶弃用 String.hashCode（规整前缀聚集严重），改用 SHA-256 摘要取桶
- Game API 补齐默认时区：`games.default_timezone`（IANA 标识，默认 UTC），DTO 校验 + Service 层 ZoneId 白名单校验
- Game 生命周期操作接入审计日志：创建/更新/删除/发布/下线全量记录（P1「审计日志」覆盖 Game 资源）
- GameServiceTest 从空壳补齐 8 个用例：默认值、时区校验、状态机、软删除级联、审计断言
- API Key 角色化：keyType 统一为 `client/server/admin`；server key 强制 HMAC，admin key 只读；创建接口支持 `keyRole` 参数
- 客户端 SDK 移除 HMAC：iOS 删除 HMACManager，Unity 移除签名代码；HMAC 仅保留给 Server SDK
- Gateway HmacFilter 加固：client key 携带签名头返回 401 `signature_not_supported`，杜绝无 secret 验签 NPE；补充 client/server key 行为测试
- RiskRule API：新增 `/api/risk-rules` CRUD + enable/disable，Specification 多条件分页查询，操作全量写入审计日志

## v0.1.0 (initial release)
- Unified Java stack for ingest + streaming + analytics
- Gateway (Spring Boot): /v1/batch NDJSON+gzip; auth/HMAC; per-key & per-IP rate limit; JSON Schema; props allowlist; PII policy (mask/drop/coarse IP); DLQ; unified errors; OTel
- Control service: H2 persistence; API+Web UI; projects & API keys; dynamic policies (ratelimit/allowlist/PII); admin token
- Streaming (Flink): enrich (validate/dedup/UA/GeoIP), sessions, retention (D0/D1/D7/D30), funnels (two-step)
- Storage (ClickHouse): events/sessions; MVs & views (events/dau/revenue/ua/os)
- BI: Superset importable bundle; E2E script to import; dashboards for events, DAU, retention, funnel, revenue, UA/OS
- SDKs: Web (TS), Android (Kotlin), Unity (C#), iOS (Swift)
- Observability: OTel Collector → Prom & Grafana; docker-compose for local
- Dev/CI: k6 load test, perf matrix/report, E2E script, Gradle CI for gateway tests & Flink assemble & Web SDK build
- Deploy: Dockerfiles, Helm chart, raw K8s manifests, deployment docs
