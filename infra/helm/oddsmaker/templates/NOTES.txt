Oddsmaker deployed.
- Gateway: svc/oddsmaker-gateway (port {{ .Values.service.gateway.port }})
- Control: svc/oddsmaker-control (port {{ .Values.service.control.port }})

Assumes external Kafka ({{ .Values.env.kafkaBootstrap }}), Schema Registry ({{ .Values.env.registryUrl }}), and ClickHouse ({{ .Values.env.clickhouseUrl }}).

Update values.yaml accordingly and run:
  helm upgrade --install oddsmaker ./infra/helm/oddsmaker -n oddsmaker --create-namespace
