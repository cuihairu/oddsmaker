apiVersion: apps/v1
kind: Deployment
metadata:
  name: oddsmaker-control
  namespace: oddsmaker
spec:
  replicas: 1
  selector: { matchLabels: { app: oddsmaker-control } }
  template:
    metadata: { labels: { app: oddsmaker-control } }
    spec:
      containers:
        - name: control
          image: your-registry/oddsmaker-control:latest
          ports: [{containerPort: 8085}]
          env:
            - name: SPRING_CONFIG_ADDITIONAL_LOCATION
              value: file:/config/application.yaml
          volumeMounts:
            - name: cfg
              mountPath: /config
      volumes:
        - name: cfg
          configMap:
            name: oddsmaker-control-config
---
apiVersion: v1
kind: Service
metadata:
  name: oddsmaker-control
  namespace: oddsmaker
  annotations: {}
spec:
  type: ClusterIP
  selector: { app: oddsmaker-control }
  ports:
    - port: 8085
      targetPort: 8085
      name: http
