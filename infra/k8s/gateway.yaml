apiVersion: apps/v1
kind: Deployment
metadata:
  name: oddsmaker-gateway
  namespace: oddsmaker
spec:
  replicas: 2
  selector: { matchLabels: { app: oddsmaker-gateway } }
  template:
    metadata: { labels: { app: oddsmaker-gateway } }
    spec:
      containers:
        - name: gateway
          image: your-registry/oddsmaker-gateway:latest
          ports: [{containerPort: 8080}]
          env:
            - name: SPRING_CONFIG_ADDITIONAL_LOCATION
              value: file:/config/application.yaml
          volumeMounts:
            - name: cfg
              mountPath: /config
      volumes:
        - name: cfg
          configMap:
            name: oddsmaker-config
---
apiVersion: v1
kind: Service
metadata:
  name: oddsmaker-gateway
  namespace: oddsmaker
  annotations: {}
spec:
  type: ClusterIP
  selector: { app: oddsmaker-gateway }
  ports:
    - port: 8080
      targetPort: 8080
      name: http
